# Script em R para calcular a amplitude de um determinado conjunto de dados

# Conjunto de dados.
conjunto <- c(-2,1,2,4,5,7, 30)

range(conjunto) #função em R que determina o valor máximo e o valor mínimo do conjunto de dados.

amplitude = (max(conjunto) - min(conjunto))
